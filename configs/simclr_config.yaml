model:
  backbone: resnet50
  projection_dim: 128

training:
  batch_size: 256
  epochs: 100
  lr: 3e-4
  weight_decay: 1e-6
  temperature: 0.5

data:
  img_size: 224
  num_workers: 4

seed: 42
  warmup_epochs: 10
  mixed_precision: true
  gradient_clip: 1.0
